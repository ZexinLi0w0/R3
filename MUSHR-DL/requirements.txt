# MUSHR-DL dependencies (modernized branch)
# Pins relaxed to be compatible with PyTorch 2.5 / Python 3.10 on JetPack 6.2.
# See ../UPGRADE_NOTES.md for the migration story.
# tensorflow>=2  # legacy code path - re-enable only if needed
# Keras>=2.6
opencv-contrib-python>=4
numpy>=1.22
pynput>=1.7
scikit-image>=0.21
tqdm>=4.60
scipy>=1.10
screeninfo>=0.8
